# CarAssist v1.1.0 交付说明

> 本次迭代围绕「闪退健壮性 + 双蓝牙 + UI 重排 + 日志根目录归档 + 版本管理」五个方向。

## 一、改动清单

### 1. 闪退健壮性加固
- **worker 线程兜底**：`CarAssistService.onCreate` 给后台 `HandlerThread` 设置 `UncaughtExceptionHandler`，
  隐藏 API 在严格 ROM 上抛出的 `Error` 不再击杀进程，巡检自动续跑。
- **启动序列分步隔离**：`runStartupSequence` 每步用 `runStep` 包裹并落盘 `▶ 步骤开始 / ✓ 完成 / ✗ 异常`，
  单步失败不阻断后续步骤（如蓝牙回连失败不影响后续音量/拉起）。
- **致命崩溃双写**：`CarAssistApp` 全局崩溃处理器额外写一行到根目录归档日志，闪退前也能留痕。

### 2. 双蓝牙适配器
- 新增 `task/BtAdapters.kt`：反射枚举车机全部蓝牙适配器
  （默认适配器 + `BluetoothManager.getAdapters()` + `BluetoothAdapter.getAdapter(int)` 兜底）。
- `BluetoothTask` 改为逐适配器独立检测/回连，**任一启用中的适配器连上设备即视为蓝牙正常**。
- UI 为每个适配器动态生成独立状态卡片 + 开关（`Prefs.isBtAdapterEnabled(index)`）。

### 3. UI 重排（驾驶员友好）
- **左侧**：实时状态卡片（每开关独占一行，名称完整显示，去掉 ①②③ 编号）+ 大号快捷按钮
  （最小高度 54dp，方便盲操作）。
- **右侧**：运行流程说明 + 运行日志。
- 中间保持参数设置。

### 4. 日志根目录归档
- 每条日志**同步实时**写入 `/CarAssist/logs/yyyy-MM-dd.log`（按日期命名）。
- 应用启动 / 跨日自动清理 **7 天前**旧日志。
- 原三通道（内存环 + 应用私有文件 + 本地广播）不变。
- 新增 `WRITE/READ_EXTERNAL_STORAGE` 权限（不限定 maxSdkVersion，确保 API 34 可写）；
  `MainActivity` 在首次未授权时补申请，授权后下次启动生效。

### 5. 版本管理
- `versionCode` 2 → **3**，`versionName` "1.0.1" → **"1.1.0"**。
- `compileSdk 34` / `targetSdk 28` 保持不变（保留 `WifiManager.setWifiEnabled` 兼容）。
- `CHANGELOG.md` 已记录本次全部变更。

## 二、涉及文件
- 新增：`app/src/main/java/com/carboot/assistant/task/BtAdapters.kt`
- 修改：`BluetoothTask.kt`、`CarAssistService.kt`、`MainActivity.kt`、`Prefs.kt`、`Status.kt`、
  `Logx.kt`、`CarAssistApp.kt`、`activity_main.xml`、`item_status_card.xml`、`strings.xml`、
  `AndroidManifest.xml`、`app/build.gradle`、`CHANGELOG.md`

## 三、构建与验证
- 本机环境无 JDK / Android SDK，**未能在此编译**，请在 Android Studio / 本机 gradle 执行：
  `./gradlew assembleRelease` 侧载到车机验证。
- 验证要点：
  1. 打开 App 不再到「启动序列开始」即闪退；
  2. 状态卡片标题完整、每行一个开关；
  3. 出现「蓝牙 1 / 蓝牙 2」两张卡，手机连上的那个显示已连接；
  4. 设备根目录 `/CarAssist/logs/` 出现当天日志文件，7 天后旧文件自动消失；
  5. 关于界面版本显示 `1.1.0 (code 3)`。
